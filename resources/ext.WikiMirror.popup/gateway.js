// TODO: get config from LocalSettings on path to REST or TextExtract API of foreign wiki
// then configure gateway to fetch from there, using builtin rendering (hopefully, somehow)
const api = mw.ForeignRest();

export function createMirrorPreviewGateway() {

}
